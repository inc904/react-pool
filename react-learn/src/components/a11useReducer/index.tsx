/* 
    - useReducer 是一个用于在函数组件中管理状态的 Hook。
    - 它接收一个 reducer 函数和一个初始状态，并返回当前状态和一个 dispatch 函数，用于更新状态。
    - 一样是管理状态，与useState不同的是 userReducer是集中式的管理状态

*/
export default function useReducerPage() {
  return <div>useReducerPage</div>
}
